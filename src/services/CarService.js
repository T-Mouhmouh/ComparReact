const CarService = {
  ShercheCar: function (marque, model, ville) {
    console.log("ddddd : " + marque, model, ville);
  },

  secondValidationMethod: function (value) {
    //inspect the value
  },
};

export default CarService;
